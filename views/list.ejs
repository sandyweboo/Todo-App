<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo</title>
    <link rel="stylesheet" href="css/styles.css" />
  </head>
  <body>
    <div class="box" id="heading">
      <h1><%= listTitle %></h1>
    </div>

    <div class="box">
      <% items.forEach((item) => { %>
        <form action="/delete" method="post">
          <div class="item">
            <input type="checkbox" name="check" value="<%= item._id %>" onchange="this.form.submit()" />
            <label for="check_<%= item._id %>"><%= item.name %></label>
          </div>
          <input type="hidden" name="listItem" value="<%= listTitle %>" />
        </form>
      <% }) %>
      
      <form action="/" method="post">
        <input type="text" name="newItem" required placeholder="Add Item" />
        <button type="submit" name="button" value="<%= listTitle %>">+</button>
      </form>
    </div>
  </body>
</html>
